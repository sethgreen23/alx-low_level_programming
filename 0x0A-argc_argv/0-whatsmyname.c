#include <stdio.h>
/**
 * main - main function
 * @argv: arguments array
 * @argc: number of elements in the array
 *
 * Return: nothing
 */
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	putchar('\n');
	return (0);
}
