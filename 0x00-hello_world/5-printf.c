#include <stdio.h>

/**
 * main - function main
 *
 * Description: print a string
 * Return: nothing
 */

int main(void)
{
	char str[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", str);
	return (0);
}
