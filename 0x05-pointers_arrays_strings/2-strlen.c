include "main.h"
/**
 * _strlen - count the length of string
 *
 * @s: the string
 *
 * Return: return the length of the string
 */
int _strlen(char *s)
{
	int count = 1;

	while (*s != '\0')
	{
		counter++;
	}

	return (counter);
}
